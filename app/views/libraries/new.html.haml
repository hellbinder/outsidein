%h1 New library
= form_for @library do |f|
  = f.label :name, "Name"
  = f.text_field :name
  = f.label :built_date, "Build Date"
  = f.number_field :built_date
  = f.submit "Create", disable_with: 'Creating...'